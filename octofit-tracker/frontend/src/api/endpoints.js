/**
 * OctoFit API Endpoints
 * Centralized endpoint definitions for frontend API routes
 */

export const API_ENDPOINTS = {
  // User endpoints
  users: '/api/users/',
  
  // Team endpoints
  teams: '/api/teams/',
  
  // Activity endpoints
  activities: '/api/activities/',
  
  // Leaderboard endpoints
  leaderboard: '/api/leaderboard/',
  
  // Workout endpoints
  workouts: '/api/workouts/',
};

export default API_ENDPOINTS;
